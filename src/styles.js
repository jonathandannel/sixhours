import { makeStyles } from '@material-ui/core/styles';

const mainContainerStyles = makeStyles(() => ({
  mainContainer: {
    display: 'flex',
    flexDirection: 'column',
    padding: '2rem',
  },
}));

export default mainContainerStyles;
